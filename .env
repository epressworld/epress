# ===========================================
# ePress Configuration Example
# ===========================================
# Copy this file to .env and modify the values according to your needs
# All configuration items use the EPRESS_ prefix for better organization
#
# NOTE: With the new web-based installation wizard, you only need to configure
# the infrastructure settings below. Application settings (node details, mail, etc.)
# will be configured through the web interface at /install on first startup.

# ===========================================
# INFRASTRUCTURE SETTINGS (Required for Server Startup)
# ===========================================
# These settings are required for the server to start and connect to the database.
# Configure these in your .env file before starting the application.

# ===========================================
# Server Configuration
# ===========================================
# Server listening address (0.0.0.0 for all interfaces)
EPRESS_SERVER_HOST=0.0.0.0

# Server port number (1-65535)
EPRESS_SERVER_PORT=8544

# Client port number (1-65535)
EPRESS_CLIENT_PORT=8543

# ===========================================
# Database Configuration
# ===========================================
# SQLite database file path
# Example: ./data/epress.sqlite
EPRESS_DATABASE_CONNECTION=./data/epress.sqlite

# ===========================================
# Frontend Proxy Configuration
# ===========================================
# API service address (used by frontend for API calls)
# This should match your server configuration
EPRESS_API_URL=http://localhost:8544

# ===========================================
# Development Configuration
# ===========================================
# Enable debug mode (true|false)
# When true, enables pretty-printed console logs and additional debugging
EPRESS_DEV_DEBUG=false

# Log level (error|warn|info|debug)
# Controls the verbosity of logging output
EPRESS_DEV_LOG_LEVEL=info

# ===========================================
# Logging Configuration
# ===========================================
# Log file path (optional)
# When set, logs will be written to this file instead of console
# Example: ./logs/epress.log
EPRESS_LOG_FILE=

# ===========================================
# APPLICATION SETTINGS (Configured via Web Interface)
# ===========================================
# The following settings will be configured through the web-based installation
# wizard at /install when you first start the application. You do NOT need to
# set these in your .env file.
#
# - Node Configuration (address, URL, title, description)
# - Authentication Settings (JWT secret, expiration)
# - Email Configuration (transport, from address)
# - Client Preferences (default language, theme)
# - WalletConnect Project ID
#
# These settings will be stored in the database and can be updated later
# through the settings interface.
